#ifndef __SPFS_INTERFACE_H_
#define __SPFS_INTERFACE_H_

#include "include/spfs_interface.h"

enum {
	SPFS_MANAGER_CMD_MOUNT_FS = SPFS_CMD_MAX,
	SPFS_MANAGER_CMD_MAX,
};

TODO:
1) Bypass spfs commands to spfs.
2) Add mount command handling to manager
3) Add mount command construction to client

#endif

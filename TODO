TODO list:
0) Replace "Golem mode" with Proxy on actual fs (tmpfs is, probably, the best?)
1) script to mount NFS and change FUSE to proxy mode.
2) Makefile "install" goal.
3) Locks support.
4) Automake.
5) RPM
6) Make sure fuse if forked beyond process pids limit to avoid collision.
